# Pila de IA (GPU NVIDIA). Instalar con el índice CUDA de PyTorch:
#   pip install -r backend/requirements-ai.txt --extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.4
torchvision
einops
opencv-python
numpy
scenedetect[opencv]>=0.6
huggingface_hub
safetensors
# Para el modo Calidad (inpainting SVD de StereoCrafter):
diffusers>=0.30
transformers
accelerate
